import { DeckSelectionComponent } from "src/app/deck-selection/deck-selection.component"

describe('deck-selection.cy.ts', () => {
  it('playground', () => {
     cy.mount(DeckSelectionComponent)
  })
})